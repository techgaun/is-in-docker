{
  "name": "is-in-docker",
  "version": "0.1.0",
  "description": "A simple module to check if the process is running inside docker or not",
  "license": "MIT",
  "repository": "techgaun/is-in-docker",
  "author": {
    "name": "techgaun",
    "email": "coolsamar207@gmail.com",
    "url": "samar.techgaun.com"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "scripts": {
    "test": "xo --space 4 index.js && mocha",
    "cover": "istanbul cover --report html node_modules/.bin/_mocha -- -u exports -R spec test.js"
  },
  "files": [
    "index.js"
  ],
  "keywords": [
    ""
  ],
  "dependencies": {},
  "devDependencies": {
    "mocha": "^2.2.5",
    "mock-fs": "^3.4.0",
    "multiline": "^1.0.2",
    "xo": "^0.9.0"
  },
  "xo": {
    "envs": [
      "node",
      "mocha"
    ]
  }
}
